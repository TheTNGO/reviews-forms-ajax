Comments
	DONE * Create comment class (with tests)
		 
	DONE * create form/controller methods
		 
	DONE * update single-review page to display comments
		 
	DONE * create Spring JPA repos
		 	DONE * Comments
		 	DONE * Tags

Tags

		* Allow user to add a tag to a single review
			* tags should not have the same name

		* Allow user to delete a tag from a single review 

	DONE* Create Tag class
	DONE* create tag REST controller
			
		
	DONE* clicking on a tag should take user to a page that displays all reviews associated with that tag
	DONE* Add mappings in ReviewController for a single tag view
		